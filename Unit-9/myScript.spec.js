//organizing tests with mocha:

describe('Test Name Suite1', () => {
    
    it('Test1 Name', () = {
        return myPromise();
            .then((res) => () {

        });
    });

    it('Test2 Name', () = {
        return myPromise();
            .then((res) => () {

        });
    });

    it('Test3 Name', () = {
        return myPromise();
            .then((res) => () {

        });
    });
})